class AppConstants {
  static const String currencySymbol = '\$';
  static const String appName = 'Smart Expense Tracker';

  // 🔧 Set this to true when building the APK for Appetize.io (bypasses camera)
  static const bool isAppetizeBuild = true;   // Change to false for real device builds
}